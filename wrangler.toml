# uber_payment_cloudflare_worker — wrangler 設定（placeholder，無真機密）
# Factory Item 1：僅建立骨架，未實作業務邏輯。真實 binding / 機密於後續工作項加入，
# 機密一律走 Cloudflare secrets（wrangler secret put），不在此檔或 .dev.vars 提交。

name = "uber-payment-cloudflare-worker"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# 本機開發用變數範例導向 .dev.vars.example；真實 .dev.vars 已被 gitignore，不會進入 git。
# 此 placeholder 僅定義服務名稱與入口，未含任何敏感資訊。
